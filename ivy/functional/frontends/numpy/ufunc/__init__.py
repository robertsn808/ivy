# flake8: noqa
from ivy.ivy import empty
from . import methods
from .methods import *

from etils.edc.dataclass_utils_test import A

from .. import identity

def accumulate(ivy.array, ivy.identity):
    for i in range(len(A)):
        t = op(t, A[i])
        r[i] = t
    return r
    return
